# Google Maps JavaScript API key (Maps JavaScript API + Places API + Directions API).
# This ships to the browser; protect it with HTTP-referrer + API restrictions, not secrecy.
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Base URL of the carrier search API (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000
